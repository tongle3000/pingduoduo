import { imagesSliders, channels, topMenus, ads, products } from './home';
import { productVariants } from './product';
import { profile } from './profile';
export default {
    banners: imagesSliders,
    channels,
    topMenus,
    ads, // 6-4 ③ rxjs 广告为
    products,
    productVariants,
    profile
    // csaes,
    // evidences,
};
